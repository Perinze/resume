spring:
  application:
    name: ${APP_NAME:unnamed}
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: ${URL:jdbc:mariadb://localhost/resume?useSSL=false&characterEncoding=utf8}
    username: ${USERNAME:user}
    password: ${PASSWORD:password}
config:
  app-id: ${APPID:dingit4vuspgz8jk7pce}
  app-secret: ${APPSECRET:xhgolkRUphy07oM6f9se79ebyn18RYU0DHHWWToxWy7PRIfo0Sw13on90DxN2G-C}
